apiVersion: v1
kind: Service
metadata:
  name: mcp-atlassian
spec:
  type: ClusterIP
  selector:
    app: mcp-atlassian
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
